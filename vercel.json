{
  "outputDirectory": "dist",
  "buildCommand": "npx expo export --platform web",
  "framework": null,
  "rewrites": [
    {
      "//": "Catch-all SPA rewrite. The negative lookahead keeps /api/* paths reaching their serverless functions in /api/ instead of falling through to index.html.",
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
