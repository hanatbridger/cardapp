export const HORIZONTAL_PADDING = 16;
export const MIN_TOUCH_TARGET = 44;
export const HEADER_HEIGHT = 180;
// Liquid-glass capsule (62 = 26 icon + 14*2 item padding + 4*2 track
// padding) + 10pt bottom offset + typical safe-area — reserve this much
// scroll padding so content never hides behind the floating bar.
export const TAB_BAR_HEIGHT = 96;
export const CARD_BORDER_RADIUS = 12;
export const LARGE_CARD_BORDER_RADIUS = 20;
